# Install in a separate Python environment with compatible PyTorch wheels, never the desktop Qt runtime.
# Install the selected llama.cpp checkout's conversion requirements there as well.
torch>=2.6,<3
transformers>=4.51,<6
peft>=0.15,<1
safetensors>=0.5,<1
sentencepiece>=0.2,<1
