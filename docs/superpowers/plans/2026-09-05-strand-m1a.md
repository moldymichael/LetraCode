# Strand M1a Implementation Plan

**Goal:** Deliver M1a for local review, keeping the installed app, live data and working model intact.

**Architecture:** Retain PySide6, SQLite and llama.cpp. Keep chats in SQLite and authoritative identity/memory in the chosen data folder's ordinary `strand/` files. Budget the actual formatted model request when supported, retaining bounded fallback behavior and complete saved tool outcomes.

**Tech stack:** Fedora 44, system Python 3.14.7, PySide6 6.11.2, SQLite 3.51.2, pytest 8.4.2; no new package dependencies.

**Spec:** User request plus `/home/miceoil/Downloads/STRAND-CONTEXT.md` and steps 1–2 of `/home/miceoil/Downloads/STRAND-BUILD-HANDOFF-DESKTOP-CODEX.md`. Those documents describe desired direction, not existing code. M1b and M2 are excluded.

## Baseline and constraints

- Source checkout: `/home/miceoil/Projects/LetraCode`, branch `fix/bound-tool-result-context`, commit `45092f1b1ed2f3a6f3361864f7d8587a87fd82c6`.
- Untracked Python caches are preserved; no tracked edits existed. No applicable AGENTS.md was found.
- Review worktree: `/home/miceoil/Projects/LetraCode-strand-m1a`, branch `codex/strand-m1a`, same starting commit.
- A sibling worktree avoids editing the original checkout merely to ignore an internal worktree folder.
- Baseline command: `QT_QPA_PLATFORM=offscreen PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider`: **78 passed in 5.80s**.
- Test app uses explicit `--data-dir`; fixtures contain synthetic text. Installer tests already redirect their destinations into temporary folders. Never run the real installer.
- One writer per checkout. Independent implementation uses separate agent worktrees, then reviewed patches are applied to this review worktree.
- No push, merge into user's branch, installation, live migration, model replacement, downloads or training.

## Task 1: Bounded requests and preserved work

Files: `engine.py`, `worker.py`, `tests/test_engine.py`, `tests/test_worker.py`.

- [ ] Add regression tests for tools/template/reply reservation, Unicode/code input, oversized batches, saved-result pagination, and resumable action-limit pauses.
- [ ] Preserve the existing context-compaction fix. Count the template-rendered request plus tools through runtime APIs when available; use a labeled conservative fallback otherwise. Never truncate core instructions or current request silently.
- [ ] Request single tool calls. For >8-call replies persist every call and paired not-executed outcome, execute none, allow one correction, then pause with a saved checkpoint. Ten-round exhaustion also pauses. Saved results can be read in bounded pages within the active chat, without re-executing actions.
- [ ] Run relevant worker/engine tests; inspect actual installed runtime endpoints before making exact-token claims.

## Task 2: Authoritative ordinary files

Files: new `strand.py`, `store.py`, new `tests/test_strand.py`, `tests/test_store.py`.

- [ ] Test global/two-project separation, fresh reads, guarded updates and Undo, symlink rejection, migration/reopen/failure, and backup/restore with temporary fixtures.
- [ ] Initialize `identity/strand.md`, `identity/preferences.md`, `memory/global.md`, `memory/projects/<id>.md`, and `learning/programming.md`. Stable entry IDs, date, scope, origin and receipts accompany tool changes.
- [ ] Migrate version 1 project memory once after a consistent database backup. Clear its old editable value; retain compatibility through file-backed Store access. Fail safely on conflicting migration destinations or unsupported future versions.
- [ ] Protect changed files with snapshots/hashes, atomic replacement and private backups; Undo refuses to overwrite a later user edit. Refuse symlink/hardlink redirection.
- [ ] Include Strand files and receipts in backups; document restoring to a new isolated data directory and retargeting linked source roots before opening a copied database.

## Task 3: Context, permissions and native UI

Files: `context.py`, `tools.py`, `ui.py`, `app.py`, new context/tool/UI tests.

- [ ] Load identity in global and project turns, re-read external corrections every turn, retrieve bounded applicable memory with explicit partial-coverage markers and read-only pagination. Model provenance comes from configuration, not guessed identity prose.
- [ ] Add app-resolved `read_memory` and `remember` operations. Default writes show exact destination/text/diff for approval; no heuristic grants based on source text. Offer an explicit user-controlled learning grant for only the programming record. No grant authorizes source edits, identity changes, commands or training.
- [ ] Connect project memory editor to files; use conflict-aware saves instead of overwriting external changes. Add global/identity/preferences/learning editor access and visible saved receipts/Undo. Keep LetraCode app branding and scope out M1b navigation.
- [ ] Acquire app data lock before migration. Exercise native Qt flows offscreen with isolated files.

## Task 4: Integration, evidence and review

- [ ] Review each integrated patch and run the full suite, compilation and whitespace checks.
- [ ] Run a serialized real-model probe with temporary data using the existing permitted configuration, recording runtime build, endpoint behavior, 32768/8192 profiles and limitations. Do not interrupt unrelated processes.
- [ ] Save dated verification evidence, a safe development launch command, and a short manual Fedora checklist. Distinguish mocked, actual inference and live visual evidence.
- [ ] Obtain independent final code review; fix material findings and rerun affected tests. Preserve a reviewable local branch/patch and stop for user review.

## Decisions and progress

The user explicitly delegated engineering choices and asked for implementation then review; no additional design approval round is needed. Memory writes use the app's existing review boundary by default because model interpretation of “remember this” is not reliable authority. A narrowly granted learning file is the only optional automatic destination. Creating additional discovery indexes, fine-tuning controls and generalized reading jobs would exceed M1a.
