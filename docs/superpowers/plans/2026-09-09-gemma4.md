# Gemma 4 integration plan

Goal: Extend existing approved-example → QLoRA → held-out comparison → GGUF adapter → Chat → rollback workflow, preserving Llama behavior and user data.

Design: Use original Google Gemma 4 E2B/E4B instruct safetensors, the installed Transformers/PEFT/bitsandbytes runtime, and the installed llama.cpp converter. Train text decoder attention/MLP projections only. Keep large frozen per-layer embeddings in system RAM if required; do not load audio/vision encoders for text training. Use FP16 compute on Turing, response-only masking with Gemma's non-thinking template, non-reentrant checkpointing and measured finite gradient/weight checks. Derive the Chat GGUF from the exact original checkpoint and require a hash-linked preparation manifest for Gemma adoption. Existing QAT GGUFs remain separate. E2B is the first empirical target; E4B has a documented 10 GB QLoRA baseline and is experimental with CPU PLE offload until measured.

Alternatives assessed: Unsloth advertises E2B 8 GB/E4B 10 GB and has relevant Gemma numerical fixes, but adds runtime patches/dependencies. Start with established runtime (Transformers 5.17 includes shared-KV fix), verify numerics, and revisit only with evidence. Full precision GPU LoRA is too large with PLE. Merged full models require additional disk/RAM and change version attribution, whereas adapter export preserves existing adoption/rollback contracts.

- [x] 1. Verify training feasibility with original E2B weights and a bounded throwaway GPU probe; record model provenance and inspect actual chat template.
- [x] 2. Add failing architecture/tokenization/offload/target contracts, extend backend with explicit Gemma branch, and retain legacy Llama tests. Enforce original safe weights, report memory and target policy, validate finite real adapter updates.
- [x] 3. Add Gemma setup guidance/profile to Fine-Tuning, update report validation, require matching Chat provenance and hash checks before adoption. Tests cover rejection/mismatch and UI settings preservation.
- [x] 4. Create reproducible original-GGUF preparation and real training verification tools using TrainingRepository, TrainingWorker, ActivationWorker and Chat worker; prove real generated candidate and rollback responses in isolated data.
- [x] 5. Run relevant and full regression suites, review focused diff, record measured GPU/system RAM, run settings, limitations and evidence. Install changed modules only after compatibility check/backups; keep existing models/data/settings.

Workspace: /home/miceoil/Projects/worktrees/active/LetraCode-gemma4, based on 7c62e95 with the existing uncommitted Hermes tools fix copied intact. The anchor checkout is older 0.1.1; do not replace it with installed files.

Completed: E2B and E4B real GPU training/export/MainWindow Chat/restart/rollback; E4B 250-token update passed,509-token probe OOM; E2B509-token probe passed. Llama CUDA fixture regression passed. Installed sources and labeled synthetic versions backed up and checked against all original database rows. See docs/GEMMA4.md and docs/GEMMA4-VERIFICATION.json for measurements and limits.
